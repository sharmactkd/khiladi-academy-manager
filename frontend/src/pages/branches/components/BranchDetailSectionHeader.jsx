import SectionHeader from "../../../components/common/SectionHeader.jsx";

const BranchDetailSectionHeader = (props) => (
  <SectionHeader classNames={{ root: "branch-detail-card__header", icon: "branch-detail-card__icon" }} {...props} />
);

export default BranchDetailSectionHeader;
