import SectionHeader from "../../../components/common/SectionHeader.jsx";

const BranchFormSectionHeader = (props) => (
  <SectionHeader classNames={{ root: "add-branch-card__header", icon: "add-branch-card__icon" }} {...props} />
);

export default BranchFormSectionHeader;
