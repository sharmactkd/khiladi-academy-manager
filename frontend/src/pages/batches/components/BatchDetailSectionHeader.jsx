import SectionHeader from "../../../components/common/SectionHeader.jsx";

const BatchDetailSectionHeader = (props) => (
  <SectionHeader classNames={{ root: "batch-detail-card__header", icon: "batch-detail-card__icon" }} {...props} />
);

export default BatchDetailSectionHeader;