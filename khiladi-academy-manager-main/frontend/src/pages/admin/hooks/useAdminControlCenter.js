import { useCallback, useEffect, useState } from "react";
import { adminApi } from "../../../api/adminApi.js";
import { planApi } from "../../../api/planApi.js";
const dataOf = (response) => response?.data?.data || {};
const useAdminControlCenter = () => { const [data, setData] = useState({ summary: {}, recentAcademies: [], academies: [], users: [], subscriptions: [], grants: [], plans: [] }); const [loading, setLoading] = useState(true); const [refreshing, setRefreshing] = useState(false); const [error, setError] = useState("");
  const load = useCallback(async ({ quiet = false } = {}) => { quiet ? setRefreshing(true) : setLoading(true); setError(""); const results = await Promise.allSettled([adminApi.getOverview(), adminApi.getAcademies({ limit: 100 }), adminApi.getUsers({ limit: 100 }), adminApi.getSubscriptions({ limit: 100 }), adminApi.getGrants(), planApi.getAll()]); const keys = ["overview", "academies", "users", "subscriptions", "grants", "plans"]; const next = {}; results.forEach((result, index) => { if (result.status === "fulfilled") next[keys[index]] = dataOf(result.value); }); setData({ summary: next.overview?.summary || {}, recentAcademies: next.overview?.recentAcademies || [], academies: next.academies?.academies || [], users: next.users?.users || [], subscriptions: next.subscriptions?.subscriptions || [], grants: next.grants?.grants || [], plans: next.plans?.plans || [] }); const failed = results.find((item) => item.status === "rejected"); if (failed) setError(failed.reason?.response?.data?.message || "Some platform data could not be loaded."); setLoading(false); setRefreshing(false); }, []);
  useEffect(() => { load(); }, [load]); const createGrant = async (payload) => { await adminApi.createGrant(payload); await load({ quiet: true }); }; const revokeGrant = async (id) => { await adminApi.revokeGrant(id); await load({ quiet: true }); }; return { ...data, createGrant, error, loading, refresh: () => load({ quiet: true }), refreshing, revokeGrant }; };
export default useAdminControlCenter;
