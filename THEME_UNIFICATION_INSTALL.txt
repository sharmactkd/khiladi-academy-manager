KHILADI LEGACY PAGE THEME UNIFICATION

Run from repository root in VS Code PowerShell.

Expand-Archive `
  -Path ".\khiladi-all-pages-theme-unification.zip" `
  -DestinationPath "." `
  -Force

Remove-Item -Recurse -Force frontend\node_modules\.vite -ErrorAction SilentlyContinue
npm run build --prefix frontend
git status --short

New files:
  frontend/src/components/common/LegacyRouteHero.jsx
  frontend/src/styles/legacyPremium.css

No old page is deleted. Existing page business logic and routes are preserved.
The compatibility layer is applied only to the explicitly mapped legacy routes.
